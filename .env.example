# Example environment variables for development and production
# Copy this file to `.env.local` for local development (do not commit secrets)

# Local development
# NEXTAUTH_URL should be your local dev origin for local testing
NEXTAUTH_URL=http://localhost:3000

# Production (Vercel)
# Set this in your Vercel project Environment Variables (Production)
# Use the canonical origin (no trailing slash)
NEXTAUTH_URL=https://discus-delta.vercel.app

# GitHub OAuth credentials (do NOT commit real secrets)
GITHUB_CLIENT_ID=your_github_client_id
GITHUB_CLIENT_SECRET=your_github_client_secret

# NextAuth secret (generate a long random string)
NEXTAUTH_SECRET=your_nextauth_secret
